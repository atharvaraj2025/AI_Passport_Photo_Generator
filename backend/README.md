# Backend

FastAPI backend for local passport photo generation. Run with:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Swagger is available at `http://localhost:8000/docs` and ReDoc at `/redoc`.
